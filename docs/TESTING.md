# 内测版验证记录

## 2026-09-21：四神补全（白虎与朱雀）

新增 ProjectK 四神白虎（0100）和朱雀（0101），内容包增至 38 位角色、76 张心象、8 张额外角色绑定牌，沿用 35 个技能拼图与规则 0.2.0-alpha。本轮未修改引擎。

- `php tests/content-pack.php`：38 场完整对局、10,294 次操作、1,746,837 项断言通过；覆盖四神齐备、新角色两技能/两心象、首张共享/次张绑定、模板与实际构筑一致及实体牌守恒。
- `php tests/puzzles.php`：126 项通过，包含已有绑定生效与失配替代、攻击响应和组合机制检查。
- `php -d extension=pdo_sqlite tools/doctor.php`：38/38 张立绘及全部运行依赖正常；新增两图均 1024×1536，提示词与 SHA256 见 `docs/content/art-prompts-four-gods.json`。
- 浏览器实际搜索“四神”得到玄武、青龙、白虎、朱雀四位，查看新增两人的完整技能/心象说明与共享、专属标识；两张卡面均成功加载，控制台错误为空。
- 内容生成器语法、重建一致性、预算校验与 diff 空白检查通过；静态玩家规则页已重新生成。

下方保留首批 36 人版本的完整验收记录。新增角色仍需真人平衡测试，自动对局只验证可执行性与状态不变量。

## 2026-09-21：加帕里内容包与创作拼图 v2

运行环境仍为 Windows / PHP 7.4.33 / PDO SQLite / Node.js 语法检查及内置 Chromium。规则版本 `0.2.0-alpha`。以下是本次最终实现的验证结果；下方 2026-09-05 部分保留作历史记录。

| 测试 | 结果 |
| --- | --- |
| `php tests/puzzles.php` | 126 项；覆盖新增积木、转换、费用、心坏、双重防御、私密观星及其后续结算、旧状态迁移与未知版本拒绝 |
| `php tests/content-audit.php` | 13 项组合审计；覆盖虚拟攻击后的等待、多段赠牌选择、偷牌资源预估及响应者出局 |
| `php tests/content-pack.php` | 36 场新角色完整对局、8,494 次操作、1,442,102 项断言；校验全部 40 份构筑、35 个技能模板、72 张心象、6 张绑定与 36 张立绘 |
| `php tests/engine.php` | 9,900 项，包含 8 场完整原角色回归对局 |
| `php tests/properties.php` | 24 场完整对局、7,723 次操作、2,690,776 项状态与实体卡守恒检查 |
| `php tests/audit-api-engine.php` | 22 项独立审计通过 |
| `php -d extension=pdo_sqlite tests/api.php` | 253 项真实 HTTP / SQLite 检查；新增开局观星不超过 45 秒及实际持久化规则版本检查 |
| 语法与环境 | 19 个 PHP 文件 lint、前端及内容生成器 JS 语法、doctor 与 diff 空白检查通过；36 张 PNG 均为 1024×1536 |
| 文档示例 | 5 份旧构筑 JSON 经新校验器通过并规范化至 0.2.0-alpha；SKILLS 中的转换技能片段装入基准构筑后通过 |

模拟对局共 68 场，分别涵盖原角色、新角色和多人数属性检查。随机洗牌会使每次操作数和断言数变化，这些数字不表示胜率或平衡结论。

本次实际浏览器操作：搜索白脸角鸮并载入工坊，将技能收藏、导出为可见 JSON 并经服务端校验导入，保存构筑后带入真实练习房；把经典“武圣”转换拼图替换到指定技能槽，确认来源、去向与次数可编辑；将豺狗主题共享心象换入白脸角鸮牌组，确认共享牌无角色绑定而博士的星图仍保留绑定。

对局中实际将观星三张牌倒序放回，后续普通摸牌依次得到所选的前两张，验证 UI 次序与服务端一致；也验证防御响应、旧对局刷新迁移日志及新房间的观星期限。390×844 手机尺寸下检查牌桌、工坊及弹窗，无页面横向溢出；恢复默认尺寸后查看角色图库。浏览器错误日志为空。浏览器验收使用单独的 `var/kf3-browser-test.sqlite`，自动 API 测试使用临时数据库。

本次仍没有真人平衡数据、真实 MySQL 部署、PHP 8.x 主机或公网验收；后续邀请测试应观察多攻击、低资源补牌、无色攻击转换及同 IP 混搭组合的强度。

## 2026-09-05：基础版历史记录

日期：2026-09-05。运行环境为 Windows、本机 PHP 7.4.33 + PDO SQLite、Node.js 语法检查，以及 Codex 内置 Chromium 浏览器。代码推荐部署到受支持的 PHP 8.2+，但本次没有将其宣称为已经完成所有 PHP 8.x 版本或真实主机的兼容测试。

## 自动验证

```sh
php tests/engine.php
php tests/audit-api-engine.php
php tests/properties.php
php -d extension=pdo_sqlite tests/api.php
node --check public/assets/app.js
php -d extension=pdo_sqlite tools/doctor.php
```

如果 PHP 已启用所需驱动，可以省略 `-d extension=pdo_sqlite`。正式游戏不需要 Node；这里只用它检查 JavaScript 语法。API 测试需要 PHP CLI 的 curl 与 proc_open，用于启动两个本地临时 HTTP worker，数据库为独立临时 SQLite，不会清空正式 `var/game.sqlite`。

最终一次完整验证：

| 测试 | 结果 |
| --- | --- |
| `tests/engine.php` | 9,974 项断言通过；包含八场完整机器人对局与逐步卡牌守恒，8场均自然结束 |
| `tests/audit-api-engine.php` | 22 项独立审计断言通过 |
| `tests/properties.php` | 24场、2～6席、两个模式，共8,270次机器人操作；每步验证卡牌守恒、唯一UID、状态范围、视角不修改状态及对局结束 |
| `tests/api.php` | 235项真实HTTP/SQLite断言通过，包括两个服务进程竞争同一房间版本 |
| PHP lint | 所有PHP源码、配置模板、测试与工具通过语法检查 |
| JS语法 | `node --check public/assets/app.js` 通过 |
| 交付环境检查 | PHP/PDO、全部源码、前端、图像和var可写检查通过 |
| 文档示例 | 五份可导入JSON均由 `Rules::validateBuild` 实际验证，包括技能说明中的完整样例 |

洗牌使用服务器随机源，因此不同次运行的具体机器人步数、赢家及断言数量会不同；不把一次样本胜率当成平衡结论。

## 有意义的边界覆盖

- 原帖普通牌的104张分布、A～K点数、限定牌与通用牌合法构筑。
- 技能白名单、严格类型、数值上限、未知字段、超额预算、过多技能/限定牌、错误点数、非法动作原子回滚。
- 防守、躲避、拳击、装备成熟、心坏、精神回复、无色削身体、系列伤害、翻面和奇迹救援。
- 延时判定、群体事件、攻守同盟反制、轮选剩牌回收、跨系列限定牌变为装备后再恢复原牌、跨玩家心象归属。
- 来源中途出局时队列继续，赠牌按归属弃置，死者不获得新手牌。
- 隐藏手牌不能通过猜测实体UID指定弃置；视角不包含对方手牌、顺序心象或未来普通牌。
- 随机身份凭证服务器只存哈希、未参与者不能窥视房间、非房主不能开局、构筑只能由所属用户修改。
- 开局快照冻结、刷新恢复、过期版本冲突、同requestId重试不重复执行、同ID异载荷拒绝。
- 两独立PHP worker同时写同revision：一个成功、另一个409，房间只增加一个版本。
- 进行中导出脱敏、反馈存储、限流、事务失败回滚、两种数据库配置路径的代码分支。

## 浏览器人工验收

在本机网页实际操作并观察结果：

- 首页插画、角色选择、私人房间与练习入口。
- 输入昵称建立身份，自动加入两名机器人并开始对局。
- 刷新后恢复原身份和房间。
- 普通牌/心象混合摸牌、永续牌装备、心象攻击与目标选择、真人响应、倒计时和机器人推进。
- 通过积木将受伤摸牌从1改为2、更名角色与限定心象、调整限定牌位置并保存；后台确认定义与顺序一致。
- 将刚保存的自创构筑带入真实练习房，牌桌显示新名字、技能与心象顺序。
- 规则与牌库、预算预览、桌面与390×844手机布局。
- 浏览器控制台检查无未处理脚本错误。
- 实际提交一条关联房间的内测反馈，确认服务端接收并显示成功提示。

联调中实际发现并修复了请求结束后动作按钮保持禁用、手机牌桌网格横向撑开、自身延时区缺失、部分响应同名卡辨识和昵称弹窗字段定位等问题。

## 尚未验证与使用边界

- **真实MySQL/MariaDB连接未验证**。PDO/InnoDB路径已实现，SQLite路径已经完整运行。若选择MySQL部署，须在你的主机上执行部署文档的首次验收步骤。
- 未访问你的虚拟主机控制面板，未进行公网部署；交付是可上传源码包和本机可运行实例。
- Apache/LiteSpeed的权限与Authorization转发需要在目标主机验收；本机PHP开发服务器不使用 `.htaccess`。
- 未做大规模并发负载、长期运行、移动Safari或旧版浏览器兼容测试。支持目标是现代Chrome/Edge/Firefox/Safari，首批小范围邀请内测。
- 没有真人平衡数据。预算只是准入规则，不保证先后手公平或所有组合强度一致。
- 全部32场自动对局验证可执行性与状态不变量，机器人只是一种有限策略，不构成穷尽性规则证明。

新增积木后应补充相应边界与组合测试，并运行整套测试；不要仅靠网页能点动就认为技能正确。
